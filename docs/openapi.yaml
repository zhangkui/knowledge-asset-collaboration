openapi: 3.0.3
info:
  title: Knowledge Asset Collaboration API
  version: 1.0.0
paths:
  /healthz:
    get:
      responses: {"200": {description: OK}}
  /api/v1/auth/login:
    post:
      responses: {"200": {description: JWT tokens}}
  /api/v1/documents:
    get:
      responses: {"200": {description: Document list}}
    post:
      responses: {"201": {description: Created}}
